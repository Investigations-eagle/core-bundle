# CoreBundle

Start demo app project `npm start` on port `localhost:42001`

Publish lib to the npm registry `npm run publish-custom`.
This will put everything from dist dir

Bug with AOT build in progress.
